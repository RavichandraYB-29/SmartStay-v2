/// Copy this file and rename it to env.dart
/// Fill in your own credentials — never commit env.dart

class Env {
  Env._();
  static const String payuMerchantKey  = 'REPLACE_WITH_YOUR_PAYU_KEY';
  static const String payuMerchantSalt = 'REPLACE_WITH_YOUR_PAYU_SALT';
  static const bool   payuIsProduction = false;
}
